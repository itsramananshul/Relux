//! Web bridge node — local HTTP/SSE endpoint feeding Relix Web (`POST /chat`).
//!
//! Forwards browser-originated chat requests into a SOL flow on the local
//! controller; streams tokens back over SSE.
//!
//! M1 stub; M9 fills.
